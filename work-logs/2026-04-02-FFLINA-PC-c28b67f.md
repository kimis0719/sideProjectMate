## 2026-04-02 — FFLINA-PC (feature/228-uiux-phase2-common-components) `c28b67f`

> 모델: claude (spm-done 자동생성)

## 작업 요약

UI/UX 전면 개편 Phase 2 완료 — 신규 디자인 시스템 토큰(surface-*/on-surface-* 계열)을 공통 컴포넌트 전체에 적용하고, No-Line Rule·glassmorphism·hover 확장 규칙을 구현하며, MobileTabBar 신규 생성

## 변경된 파일

### 전역 스타일
- `src/app/globals.css` — btn-primary → bg-primary-container + hover 가로 4px 확장; btn-tertiary/btn-error 신규 추가; card No-Line Rule(border 제거); form-input Ghost Border; badge-* 신규 토큰으로 교체

### 공통 컴포넌트 (스타일 교체)
- `src/components/common/Toast.tsx` — bg-card/border-border → bg-surface-container-lowest/shadow-modal; 아이콘 컬러 신규 토큰
- `src/components/common/Skeleton.tsx` — bg-muted → bg-surface-container-high; Card/ListItem No-Line Rule
- `src/components/common/EmptyState.tsx` — text-foreground/muted → text-on-surface/on-surface-variant
- `src/components/common/GlobalModal.tsx` — backdrop glassmorphism(backdrop-blur-16px + surface 80% opacity); bg-card → bg-surface-container-lowest; 버튼 영역 No-Line Rule
- `src/components/common/TagInput.tsx` — border-input/bg-background → Ghost Border/bg-surface-container-lowest; 추천 칩 신규 토큰

### 신규 생성
- `src/components/common/MobileTabBar.tsx` — 모바일 하단 탭 바(4개 탭, 라우트 기반 노출, iOS safe-area env())

### 페이지 컴포넌트
- `src/components/projects/ProjectCard.tsx` — 카드 No-Line Rule(border 제거, hover shadow-ambient); 상태 뱃지 인라인 하드코딩 컬러 → 신규 토큰; 텍스트 on-surface 계열 적용
- `src/components/Header.tsx` — 전체 색상 토큰 교체(muted-foreground→on-surface-variant, bg-muted→surface-container-high 등); 알림/유저 드롭다운 No-Line Rule; 모바일 패널 surface-container-lowest; 에러 뱃지 bg-error
- `src/components/Footer.tsx` — bg-accent → bg-surface-container-low; 텍스트/링크 on-surface 계열; No-Line Rule

### 레이아웃
- `src/app/layout.tsx` — MobileTabBar import + 조건부 렌더링; main에 pb-16 md:pb-0 추가(탭 바 높이 확보)

## 테스트 결과

- 실행 명령: `npm run test:run`
- 결과: 456 passed / 0 failed
- 신규 추가 테스트: 0개
- 미작성 테스트 및 사유: 없음 (스타일/컴포넌트 교체만, 로직 변경 없음)

## 건드리면 안 되는 부분

| 파일 | 위치 | 이유 |
| --- | --- | --- |
| `globals.css` | 기존 CSS 변수 블록 (`--background`, `--foreground`, `--primary` 등) | 기존 컴포넌트 전체가 이 토큰에 의존 — Phase 3+ 컴포넌트 교체 완료 전까지 제거/수정 금지 |
| `globals.css` | `.btn-secondary`, `.btn-destructive` | 아직 기존 코드에서 사용 중 — Phase 3+ 교체 완료 전까지 제거 금지 |
| `Header.tsx` | socket 이벤트 핸들러 (`new-notification`, `message-received`), `join-user-room` emit | 실시간 알림/채팅 로직 — 로직 수정 금지, className 교체만 허용 |

## 미완성 / 다음 세션에서 이어받을 부분

- Phase 3: 칸반 보드 (3-1 디자인 다듬기 → 3-2 InboxPanel 신규 생성 → 3-3 KanbanMobileView 신규 생성 → 3-4 AI 플로팅 바 → 3-5 노트 마크다운 첨삭)
