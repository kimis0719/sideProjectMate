## 2026-03-31 — FFLINA-PC (feature/215-redesign-phase4) `7a9560a`

> 모델: claude (spm-done 자동생성)

## 작업 요약

SPM 서비스 전면 개편 Phase 4 완료 — next-auth 세션에 onboardingStep 주입, middleware 온보딩 redirect, /onboarding 3스텝 페이지(DOMAIN 카드선택 + CommunicationStyleSlider + AvailabilityScheduler), /profile OnboardingWizard 모달 제거, /mypage 프로필 수정 링크 추가, 소켓 루프/Image sizes warning 등 버그 수정

## 변경된 파일

### 인증/미들웨어
- `src/types/next-auth.d.ts` — Session/JWT/User에 onboardingStep 타입 추가
- `src/lib/auth.ts` — jwt callback에서 DB onboardingStep 주입, 기존 세션 fallback, trigger='update' 지원
- `src/middleware.ts` — 신규: onboardingStep < 4 시 /onboarding redirect, api/socket/ 경로 제외

### 온보딩
- `src/app/api/users/onboarding/route.ts` — 신규: PATCH(스텝별 저장), POST(건너뛰기)
- `src/app/onboarding/page.tsx` — 신규: 3스텝 온보딩 페이지 (DOMAIN + CommunicationStyleSlider + AvailabilityScheduler)

### 프로필/마이페이지
- `src/app/profile/page.tsx` — OnboardingWizard 모달 제거 (온보딩은 middleware 처리)
- `src/app/mypage/page.tsx` — "프로필 수정하기" → /profile 링크 추가, any 타입 수정

### 버그 수정
- `src/components/Header.tsx` — 소켓 useEffect dependency session → session?.user?._id, any 타입 제거
- `src/components/profile/AvailabilityScheduler.tsx` — '금' 셀 크기 불일치 (table-layout: fixed)
- `src/components/projects/ProjectCard.tsx` — 이미지 없을 때 항상 썸네일 영역 표시
- `src/app/projects/[pid]/page.tsx` — '목록으로' 뒤로가기 버튼 추가, 중복 관리 버튼 제거
- `src/components/projects/ProjectThumbnail.tsx` — Image sizes prop 추가
- `src/components/profile/DetailProfileCard.tsx` — Image sizes prop 추가
- `src/components/ProjectImageSlider.tsx` — 슬라이드 wrapper 구조 개선

### 계획서
- `docs/plans/SPM_REDESIGN_PLAN.md` — Phase 4 온보딩 스텝/프로필 레이아웃/완성도 가중치 수정 (AvailabilityScheduler/CommunicationStyleSlider 유지 반영)

## 설계 결정

- **CP-3-2 확정**: /profile = 공개 프로필(편집 포함), /mypage = 계정 설정 + 프로필 수정 링크
- **CP-4-1 확정**: 미들웨어 예외 경로에 /admin 추가
- **CP-4-2 확정**: 온보딩 완료 후 /projects로 redirect
- **AvailabilityScheduler/CommunicationStyleSlider 유지**: weeklyAvailability(숫자)/workStyle(enum) 대신 기존 컴포넌트 재사용
- **소켓 루프 원인**: middleware가 WebSocket upgrade 요청을 처리하지 못함 → matcher에서 api/socket/ 제외

## 테스트 결과

- 실행 명령: `npm run test:run`
- 결과: 653 passed / 0 failed
- 신규 추가 테스트: 0개
- 미작성 테스트 및 사유: 온보딩 API 테스트, middleware 테스트 미작성 (Phase 5에서 보완 예정)

## 건드리면 안 되는 부분

| 파일 | 위치 | 이유 |
| --- | --- | --- |
| middleware.ts | api/socket/ matcher 제외 | WebSocket upgrade와 Next.js middleware 비호환 — 제거 시 소켓 무한 루프 재발 |
| auth.ts | jwt callback 기존 세션 fallback | onboardingStep undefined 시 DB 조회 로직 — 제거 시 기존 유저 redirect 루프 |
| auth.ts | trigger='update' 처리 | 온보딩 완료 시 세션 갱신에 필수 — 제거 시 무한 redirect |

## 미완성 / 다음 세션에서 이어받을 부분

- Phase 5: 지원 플로우 개편
- 프로필 완성도 게이지 UI (4-C 일부 미구현 — Phase 5 이후 별도 이슈로 처리)
- next/image sizes prop: Header.tsx UserAvatar 외 나머지 프로필 컴포넌트 일부 (lint-staged stash 과정에서 누락 가능성)
