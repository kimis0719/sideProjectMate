## 2026-04-18 — kimis0719 (feature/SPM-25-codex-skill-enhancement) `48b6e10`

> 모델: codex (spm-done 수동 생성)

## 작업 요약

SPM-25 코덱스 스킬 구조 정리와 종료 절차 보완을 마무리하고, 테스트 플래키 이슈(메모리 Mongo 기동 타임아웃)를 안정화했다.

## 변경된 파일

- `.agents/skills/**`, `.claude/skills/**`
  - 기본 디자인 계열 스킬을 정리하고 SPM 관련 스킬(`spm-start`, `spm-commit`, `spm-done`, `spm-revert`, `spm-hotfix`) 중심으로 재구성
- `AGENTS.md`
  - Codex 에이전트용 프로젝트 규칙 동기화
- `CLAUDE.md`
  - SPM-25 작업 현황 반영 및 종료 시 현황 표 복원 대상 업데이트
- `vitest.config.ts`
  - `hookTimeout`, `testTimeout`를 30초로 상향해 CI/로컬 간헐 타임아웃 완화
- `src/__tests__/helpers/mockDb.ts`
  - `MongoMemoryServer.create()` 재시도 로직(최대 3회) 및 초기화 실패 가드 추가

## 테스트 결과

- 실행 명령: `npm.cmd run test:run`
- 결과: 580 passed / 0 failed
- 실행 명령: `npx.cmd tsc --noEmit`
- 결과: 통과
- 신규 추가 테스트: 0개
- 미작성 테스트 및 사유: 없음 (기존 테스트 인프라 안정화 변경)

## 건드리면 안 되는 부분

| 파일 | 위치 | 이유 |
| --- | --- | --- |
| `.agents/skills` | Junction(.claude/skills 연결) | 단순 디렉터리로 되돌리면 스킬 동기화 구조가 깨져 삭제/중복 변경이 다시 발생할 수 있음 |
| `src/__tests__/helpers/mockDb.ts` | `setupTestDB()` 재시도 블록 | 재시도 제거 시 `mongodb-memory-server` 기동 지연 환경에서 간헐 실패가 다시 발생할 가능성 높음 |
| `vitest.config.ts` | `hookTimeout`, `testTimeout` | 기본값으로 낮추면 beforeAll 훅 타임아웃으로 API 테스트 스위트가 불안정해짐 |

## 미완성 / 다음 세션에서 이어받을 부분

- `gh auth status` 기준 GitHub CLI 토큰이 만료 상태로 확인됨. PR 생성 전 `gh auth login -h github.com` 재인증 필요.
