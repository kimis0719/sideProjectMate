## 2026-04-11 — kimis (feature/275-harness-100-integration) `c4c2df9`

> 모델: claude (spm-done 자동생성)

## 작업 요약

Harness 100 버그픽스 — 프리셋 일반유저 미표시 수정 + 지원자 역순 정렬 + 멤버 강퇴 기능 추가

## 변경된 파일

### 수정
- `src/components/board/InstructionModal.tsx` — 프리셋 fetch를 admin API + 공개 API 병렬 호출로 변경 (일반유저도 프리셋 표시)
- `src/app/api/projects/[pid]/applications/route.ts` — 지원자 목록 `.sort({ createdAt: -1 })` 추가 (최신 지원자 먼저)
- `src/app/projects/[pid]/manage/page.tsx` — 멤버 카드에 hover 강퇴 버튼 추가 + handleKickMember 핸들러

### 신규
- `src/app/api/projects/[pid]/members/[userId]/route.ts` — DELETE 멤버 강퇴 API (오너 전용, members $pull + Application status 변경)

## 테스트 결과

- 실행 명령: `npm run test:run`
- 결과: 528 passed / 0 failed
- 신규 추가 테스트: 0개
- 미작성 테스트 및 사유: 멤버 강퇴 API 단위 테스트는 다음 세션에서 추가 가능

## 건드리면 안 되는 부분

| 파일 | 위치 | 이유 |
|------|------|------|
| members/[userId]/route.ts | $pull members + Application status 변경 | 멤버 제거와 Application 상태가 연동됨 — 한쪽만 변경 시 데이터 불일치 |
| manage/page.tsx | handleKickMember() confirm 패턴 | 비가역 작업의 이중 확인 로직 — confirm 제거 시 실수로 강퇴 가능 |

## 미완성 / 다음 세션에서 이어받을 부분

- 멤버 강퇴 API 단위 테스트
