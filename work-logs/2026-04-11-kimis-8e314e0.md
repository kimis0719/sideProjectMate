## 2026-04-11 — kimis (feature/275-harness-100-integration) `8e314e0`

> 모델: claude (spm-done 자동생성)

## 작업 요약

일반유저 프리셋 미표시 버그 수정 — GET /api/ai/default-presets 공개 API 신규 생성하여 AiSettings.defaultPresets를 로그인 유저 모두 접근 가능하도록 분리

## 변경된 파일

### 신규
- `src/app/api/ai/default-presets/route.ts` — GET 기본 프리셋 공개 API (AiSettings.getInstance()에서 defaultPresets 반환)

### 수정
- `src/components/board/InstructionModal.tsx` — 프리셋 fetch를 /api/ai/default-presets 단일 호출로 단순화

## 테스트 결과

- 실행 명령: `npm run test:run`
- 결과: 528 passed / 0 failed
- 신규 추가 테스트: 0개
- 미작성 테스트 및 사유: default-presets API는 AiSettings.getInstance() 위임만 하는 thin wrapper로, AiSettings 테스트에서 커버됨

## 건드리면 안 되는 부분

| 파일 | 위치 | 이유 |
|------|------|------|
| default-presets/route.ts | AiSettings.getInstance() 호출 | 싱글턴 인스턴스 패턴 — 직접 DB 쿼리로 변경 시 캐시 무효화 문제 |
| gemini.ts | fallback 분기 (429/503/stream 에러 패턴) | 모델 폴백 체인 — 패턴 제거 시 전체 서비스 중단 |
| buildAiContext.ts | isSensitiveResource() 필터 | 환경변수/시크릿 노출 방지 가드레일 |
| members/[userId]/route.ts | $pull members + Application status 변경 | 멤버 제거와 Application 상태 연동 |
| HarnessCatalog.ts | filesCache ko/en 커플링 | 100개 문서 인덱싱 효율 유지 |

## 미완성 / 다음 세션에서 이어받을 부분

없음
