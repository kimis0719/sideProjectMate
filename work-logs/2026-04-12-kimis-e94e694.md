## 2026-04-12 — kimis (fix/SPM-9-kanban-floating-position) `e94e694`

> 모델: claude (spm-done 자동생성)

## 작업 요약

칸반보드 메모 줄바꿈이 화면 높이를 초과할 때 플로팅 요소(줌 컨트롤러, AI 버튼, 미니맵)가 위로 밀려나는 현상 수정 — `<main>` 컨테이너의 `overflow-hidden`을 `overflow-clip`으로 변경하여 브라우저 자동 스크롤 완전 차단

## 변경된 파일

### 수정
- `src/components/board/BoardShell.tsx` — `<main>` 컨테이너의 `overflow-hidden` → `overflow-clip` 변경 (L787)

## 테스트 결과

- 실행 명령: `npm run test:run`
- 결과: 567 passed / 0 failed
- 신규 추가 테스트: 0개
- 미작성 테스트 및 사유: CSS 속성 변경(overflow-hidden → overflow-clip)으로 DOM 동작 테스트는 브라우저 환경에서만 검증 가능. vitest/jsdom 환경에서는 스크롤 동작을 재현할 수 없음.

## 건드리면 안 되는 부분

| 파일 | 위치 | 이유 |
|------|------|------|
| BoardShell.tsx | `<main>` 컨테이너의 overflow-clip (L787) | overflow-hidden으로 되돌리면 메모 줄바꿈 시 브라우저 자동 스크롤이 발생하여 플로팅 요소(줌, AI버튼, 미니맵)가 위로 밀려남 |

## 미완성 / 다음 세션에서 이어받을 부분

없음
